# Data snapshot

The committed files are the OpenAlex aggregate inputs used by the published article. They were fetched together on 2026-06-19. `manifest.json` records file sizes and SHA-256 hashes.

OpenAlex updates its records and topic assignments over time. A fresh fetch can therefore differ from this snapshot.
